# Source Excel workbook (company internal — kept local, not pushed)
*.xlsx
*.xlsm
*.xls

# Brand logo source images (already embedded as base64 in Dashboard.html)
LOGO/
master_data.json

# Local updater secrets & browser profile (NEVER push these)
updater/credentials.txt
updater/line_config.json
updater/.line_alert_state.json
updater/browser_profile/

# OS / editor cruft
Thumbs.db
desktop.ini
.DS_Store
*~
*.tmp
